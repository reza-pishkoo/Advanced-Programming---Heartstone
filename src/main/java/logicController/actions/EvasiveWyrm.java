package logicController.actions;

import cards.Minion;
import model.PlayerModel;

public class EvasiveWyrm extends MinionObserver{

    public EvasiveWyrm(PlayerModel player, Minion card) {
        super(player, card);
    }
}
