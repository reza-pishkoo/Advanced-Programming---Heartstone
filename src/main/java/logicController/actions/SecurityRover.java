package logicController.actions;

import cards.Minion;
import model.PlayerModel;

public class SecurityRover extends MinionObserver{


    public SecurityRover(PlayerModel player, Minion card) {
        super(player, card);
    }
}
