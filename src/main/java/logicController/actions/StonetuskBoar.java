package logicController.actions;

import cards.Minion;
import model.PlayerModel;

public class StonetuskBoar extends MinionObserver{

    public StonetuskBoar(PlayerModel player, Minion card) {
        super(player, card);
    }
}
