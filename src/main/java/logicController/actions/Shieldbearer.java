package logicController.actions;

import cards.Minion;
import model.PlayerModel;

public class Shieldbearer extends MinionObserver{


    public Shieldbearer(PlayerModel player, Minion card) {
        super(player, card);
    }
}
