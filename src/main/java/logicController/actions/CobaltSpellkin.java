package logicController.actions;

import cards.Minion;
import model.PlayerModel;

public class CobaltSpellkin extends MinionObserver{

    public CobaltSpellkin(PlayerModel player, Minion card) {
        super(player, card);
    }
}
